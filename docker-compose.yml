version: '3'

services:
  web:
    image: web_project
    container_name: web
    ports:
      - "80:80"
    networks:
      - one_net
  mysql:
    image: mysql_server
    container_name: mysql
    ports:
      - "3306:3306"
    networks:
      - one_net

networks:
  one_net:
    driver: bridge
